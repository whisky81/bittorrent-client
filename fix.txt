1: đôi khi gặp lỗi chưa tải xong nhưng vẫn hiện 100% 
    và chuyển sang seeding ở phía ui nhưng phần files khi click vào vẫn hiện tiến trình đúng
    lưu ý: đảm bảo hiển thị chính xác thông tin phía ui
2: loại bỏ ghi log vào file pytorrent.log các thông tin 
    như [INFO] werkzeug: 127.0.0.1 - - [27/Mar/2026 14:44:05] "GET / HTTP/1.1" 200 -
    lưu ý: có thể không cần ghi log vì khi chạy có thể có hàng triệu entry được ghi khi chạy client chưa đầy 10 phút 
3: Sử dụng port động (sau đó chuẩn hóa về dải BitTorrent) để tránh xung đột khi seeding 
4: xử lý race condition và deadlock khi dùng chung download manager 
5: limit số lượng seeding 
6: tìm và thay thế polling bằng sse nếu cần thiết để đảm bảo tối ưu tài nguyên không tràn memory 
    vì sse tôi ưu tài nguyên hơn 